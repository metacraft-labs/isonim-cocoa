import XCTest

class IsoNimCocoaTests: XCTestCase {
    func testPlaceholder() {
        // Placeholder — real tests will call into the Nim library
        XCTAssertTrue(true, "IsoNim Cocoa test harness loaded")
    }
}
